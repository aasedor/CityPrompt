# Validation-only dependencies for tools/archetype_compiler (Blender brings its own Python).
trimesh==4.5.3
numpy>=1.26
# import_manifest.py (upload compiled families to the backend)
requests>=2.31
# generate_textures.py (texture library generation; the backend venv already has both)
pillow>=10.0
google-genai>=1.0
