/**
 * Vertical park assets sit on the same raised plane as the AI/deterministic
 * ground drape. Runtime park-kit GLBs must keep their ground-contact origin at
 * z=0 and must not include a duplicate court, path, or planting surface.
 */
export const PARK_DRAPE_SURFACE_LIFT_METERS = 0.32;
