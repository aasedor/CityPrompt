=== PROMPT DUMP: Pass 2 Building — Mapbox pipeline (oblique aerial) ===
Source             : frontend/src/components/viewer/useAIRender.ts
Builder            : buildBuildingPrompt (line 1628) -> buildSCHEMAPrompt mode='building' (line 1832)
Zone line builder  : buildCompressedZoneLabel (line 1750)
Archetype info     : getZoneArchetypeInfo (line 2341) + catalog lookup by development_archetype_id
Camera angle       : oblique aerial — HARDCODED in buildSCHEMAPrompt line 1866 as "drone 60m"
                     (the actual map pitch/bearing/zoom are NOT read into the prompt)
Archetype fixture  : brownstone_rowhouse_frontage from frontend/src/data/buildingArchetypes.json:331
                     Variant: brownstone_rowhouse_red_sandstone (selected_variant=0)
Zone props         : floors=3, height_m=12, polygon ~18m × 8m, user description_text = "" (empty)
Style              : photorealistic
Zone color         : #E03C31 (default red, normalised via colorName -> "red")
Reconstructed      : YES — captured logs not found in repo; constructed from code paths.

-------------------- ASSEMBLED PROMPT (Pass 2, oblique) --------------------
STYLE: Hyper-realistic exterior architectural rendering with cinematic lighting.
COMPOSITION: Oblique aerial, drone 60m, single building on red footprint, full 3D mass extending into sky
LIGHTING: Golden hour, warm southwest sun, crisp architectural shadows
CONTEXT: Preserve all unmasked satellite imagery exactly as-is. Rendered zones must blend naturally at edges — match tones, lighting, and scale of adjacent satellite context.
ZONES:
1. [red] Brownstone Rowhouse Frontage | 3F 12m | 18m×8m | warm brown sandstone+carved brownstone door surrounds, warm brown sandstone+carved brownstone door surrounds+ashlar, flat+low stone parapet
MANDATORY: Each zone renders ONLY within its colored polygon boundary. Realistic rooftop materials — no colored polygon fill visible on any surface. Replace ALL colored overlays with appropriate materials. If a zone is partially occluded by a foreground building, render only the visible portion — do not relocate the zone to a clear area. All rendered changes must remain strictly within the site boundary polygon; preserve the satellite imagery exactly as-is beyond it. THE POLYGON EDGES ARE THE BUILDING'S EXTERIOR WALLS. The building MUST FILL the entire polygon area — its footprint must match the polygon shape and size exactly, edge-to-edge. Do NOT render landscaping, driveways, plazas, gardens, or any unused space between the building and the polygon edge. Do NOT place a smaller building inside a larger polygon. The building's floor plan conforms to the polygon's shape. Archetype metadata (floor count, height, nominal dimensions) describes STYLE, MATERIALS, and CHARACTER only — adjust facade rhythm, window density, roof pitch, and ornamental density to suit the polygon's actual footprint, but the footprint itself MUST equal the polygon.
PROHIBITIONS: buildings extending beyond polygon boundaries, colored polygon fills visible on rooftops or facades, boundary lines or outlines visible in final image, people or pedestrians, text overlays or watermarks, relocating partially-occluded zones to a more visible area instead of rendering only the visible portion

-------------------- NEGATIVE PROMPT (appended by backend render.py:470) --------------------
Do NOT include: cartoon, illustration, sketch, low quality, blurry, text, watermark, neon, unrealistic colors, modern glass, building extending beyond footprint, architecture outside polygon, walls outside boundary, building bleed into neighboring zone, people, pedestrians, human figures, faces, crowds, cyclists, joggers, zone bleeding into neighboring zone, landscape extending beyond polygon boundary, elements crossing polygon edges, mismatched zone content

-------------------- BACKEND-INJECTED LABELS (render.py:378-472) --------------------
(These prefix-label the attached images and are concatenated before the text prompt above.)
Image 1 (SPATIAL LAYOUT): This is a 3D clay massing model showing the exact spatial arrangement of all structures from the camera's perspective. Use this as the definitive spatial reference.
  [inline: building_screenshot.png — map composite with colored polygon + white headroom]
Image 2 (ARCHETYPE REFERENCE — Brownstone Rowhouse Frontage): Apply the exact architectural style, materials, and textures from this reference image to the corresponding zone. Located in the red zone.
  [inline: variant_0.png — brownstone hero card, 512px compressed JPEG]
The following black-and-white mask shows the exact area to edit (white = edit, black = keep unchanged):
  [inline: buildingMask.png]
