=== PROMPT DUMP: Pass 2 (single-shot) — Globe — SCENARIO 3 NEAR-SQUARE 15m × 14m ===
Source           : frontend/src/components/viewer/globe/useGlobeAIRender.ts
Builder          : buildPrompt (line 744)
Archetype        : brownstone_rowhouse_frontage — expects LONG FRONTAGE (facade rhythm +
                   adjacent party walls). See roofDetail.aerialAppearance: "dark flat
                   surface with occasional brick chimneys and stone parapet walls".
Polygon          : 4 vertices, near-square 15m × 14m (area ~210m²)
Camera           : pitch=45° oblique
Reconstructed    : YES

-------------------- WHAT CHANGES vs SCENARIO 1 --------------------
Same non-polygon lines as before. Differences:
  - Mask: near-square white shape (~15m × 14m + 2m dilation).
  - Pixel coords token: polygon@(830,515)(950,515)(950,628)(830,628)
  - `~150m²` archetype token is now 30% smaller than the polygon's actual 210m² area —
    the zone line UNDER-represents polygon size and nothing corrects it.
  - Floors (3) and height (12m) pulled from zone props, both from user input, not polygon.

-------------------- PROBLEM WITH ARCHETYPE IMAGE vs SQUARE POLYGON --------------------
Archetype image (Image 2) shows a rowhouse that is ONE bay wide → it tiles horizontally
with neighbours. A square 15×14 polygon is NOT a rowhouse cell — it's a corner building
or a small detached townhouse.

Gemini sees:
  - Text: "Brownstone Rowhouse Frontage" (archetype name)
  - Text: "Keep the exact same building footprint and height" (archetype renderPrompt)
  - Text: "3-4 storey townhouse rhythm with stoop entries" (archetype renderPrompt)
  - Image: a rowhouse terrace (multiple narrow adjacent dwellings)
  - Mask: a square block with NO adjacent rowhouse neighbours

There is no text that tells Gemini "the polygon shape is authoritative — stretch the
archetype to fit". There is no "orientation" hint. The word "square" does not appear.

Expected failure: Gemini renders EITHER (a) a single squarish detached townhouse (archetype
lost) OR (b) a rowhouse but awkwardly proportioned, with the stoop/arched-doorway
compressed into one side and dead facade on the other. Neither matches the image.

-------------------- MAPBOX HAS, GLOBE LACKS --------------------
Mapbox useAIRender.ts:1771-1772: `footprint = ${entry.footprintDims.length}m×${entry.footprintDims.width}m`.
Globe: NO equivalent. Zone line carries archetype nominal `~150m²`, not polygon `~210m²`.

Mapbox useAIRender.ts:1820 (after commit b9cea11):
  "THE POLYGON EDGES ARE THE BUILDING'S EXTERIOR WALLS. The building MUST FILL the entire
  polygon area — edge-to-edge."
Globe: MANDATORY line (useGlobeAIRender.ts:871) reads instead:
  "Replace the colored polygon overlays visible in the screenshot with photorealistic
   architectural materials. Read the text label on each polygon to identify what to render
   there."
  — This can be read as "paint over the polygon with building materials" (permissive; a
  smaller building inside the polygon with surrounding landscape-fill would satisfy it),
  rather than "walls == polygon edges" (strict).
