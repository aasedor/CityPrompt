=== PROMPT DUMP: Pass 2 Building — Google 3D Tiles globe pipeline (oblique) ===
Source             : frontend/src/components/viewer/globe/useGlobeAIRender.ts
Builder            : buildSingleZonePromptForPerZone (line 1152) — called from renderPerZone (line 1843)
Archetype info     : getZoneArchetypeInfo (line 676) — different shape from Mapbox version
Camera angle       : Globe pipeline does NOT pass the camera angle into the per-zone prompt.
                     The buildPrompt function (line 744) DOES inject a pitchDesc, but
                     buildSingleZonePromptForPerZone does NOT. The camera angle is effectively
                     dropped for Pass 2 per-zone rendering.
Archetype fixture  : brownstone_rowhouse_frontage (variant 0), same as the Mapbox file
Zone props         : floors=3, height_m=12, polygon ~18m × 8m
Reconstructed      : YES — captured logs not found; constructed from code paths.

-------------------- BASE SCREENSHOT ANNOTATION (useGlobeAIRender.ts:1781-1819) --------------------
Before the prompt, a LABELED screenshot is generated: zone archetype names are drawn as
bold 28px white-with-black-outline text at each zone centroid (all caps). This happens
ONCE at Pass 1 and the labeled image is reused across every per-zone call.

-------------------- ASSEMBLED PROMPT (Pass 2, globe, one building) --------------------
STYLE: Photorealistic architectural visualization, photomontage quality, golden hour afternoon sunlight, sharp detail on materials and facades.
TASK: Render ONE building in the white masked area. The colored polygon [red] marks the footprint.
BUILDING: Brownstone Rowhouse Frontage | 3 floors, 12m tall
DETAILS: warm brown sandstone, carved brownstone door surrounds and window, elevated stoop entry with carved stone steps, arched doorway with fanlight transom, under-stoop garden level with iron gate, tall double-hung windows with stone lintels and sills, 2-over-2 or 4-over-4 divided
CONTEXT: Preserve ALL existing photographic context outside the mask. Match lighting, color temperature, and atmosphere of surrounding real buildings.
MANDATORY: Replace the colored polygon with a photorealistic building. Render ONLY within the masked area. Match scale and density of surrounding real 3D buildings.

ARCHETYPE STYLE REFERENCE: Image 2 shows the exact style for this zone. Replicate this within the masked area.

-------------------- NEGATIVE PROMPT (useGlobeAIRender.ts:1898) --------------------
cartoon, illustration, sketch, low quality, blurry, text, watermark, colored polygon fill

-------------------- BACKEND-INJECTED IMAGE LABELS (render.py:420-452) --------------------
Image 1 (SPATIAL LAYOUT): This is a 3D clay massing model showing the exact spatial arrangement of all structures from the camera's perspective. Use this as the definitive spatial reference.
  [inline: labeled screenshot — 3D-Tile capture with bold zone labels painted over centroids]
Image 2 (ARCHETYPE REFERENCE — Brownstone Rowhouse Frontage (front entrance facade)): Apply the exact architectural style, materials, and textures from this reference image to the corresponding zone. Located in the red zone.
  [inline: variant_0.png — brownstone hero card, 512px compressed JPEG]
The following black-and-white mask shows the exact area to edit (white = edit, black = keep unchanged):
  [inline: single-zone binary mask]

-------------------- KEY OBSERVATION --------------------
Backend labels the Google 3D Tiles screenshot as "3D clay massing model" (render.py:420). It
is NOT a clay massing model in the globe case — it is a full photographic 3D Tiles capture
with painted-on labels. See contradictions table. Also note: the camera pitch that the
SCHEMA builder computes at line 752 ("pitchDeg < 20 → near top-down") is NEVER forwarded to
Pass 2 per-zone. Only the legacy single-shot buildPrompt uses it.
