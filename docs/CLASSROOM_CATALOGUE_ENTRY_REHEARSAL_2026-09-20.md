# Catalogue runtime-entry rehearsal — 20 September 2026

This is a rehearsal of the [shared runtime checklist](ARCHETYPE_RUNTIME_INTEGRATION.md)
on three **existing pilot variants**, not approval to activate another family
or publish the Wave 2 catalogue. It identifies evidence a new building,
street or park must bring before bounded expansion. The ordinary student
controls were exercised on a disposable vacant Currie project,
`0320bb4f-395c-41c6-a6c0-ad28bb1572ef`; the protected original
`f5bffc94-def9-4c43-942e-9ae7411872e9` was not edited.

## Locked candidates and runtime evidence

| Candidate | Exact runtime identity | Observed classroom behavior | Still needed for a new variant |
| --- | --- | --- | --- |
| Building | `infill_home` picker, `infill_flat_roof_minimal` selected variant, RLASM 6.1 architectural clay, 12 × 16 m native plot, 8.45 × 11.75 × 6.98001 m model envelope | UI place/90° street-facing orientation, working Move handle, Undo/Redo/reload, front native-step pick, 1.8 m clear route, entrance review and exact preview passed on prepared ground. Wrong rear step received actionable facing guidance. Shared Generate to 3D also passed the seven-house copy after planning-path repair. | Lock the exact asset bytes and entrance measurements per new variant; test supported size/rotation range, low views and terrace/step join. This pilot does not grant RLASM keeper or accessibility approval. |
| Street | `yield_street_v0`, 6 m shared street with flush pedestrian edges | Two-point route authored in UI, saved/reopened, visible in exact scene. Building and park connection solvers used its pedestrian edge. A boundary crop that excluded it returned a readable Street #1 label and preserved saved geometry. | New sections need asymmetric/reversed metric bands, bends/junctions, crossing clearances, curb or flush semantics, full grade and capture review. No junction family was approved here. |
| Park | `neighborhood_park_v0`, `adaptive_rustic_v1` / `neighborhood-rustic-v5`, 40 × 35 m default; 30 m minimum width | UI placement, prepared-level scene and exact preview passed. On partial natural ground the default plot was hidden. Resize to 30 m and Move onto clear ground gave 1/1 repeatable park profile; west entrance to shared street saved and solver connected. | Test minimum-size programme explicitly: this 30 × 35 m recovery omitted play pocket, pavilion and swing. Natural partial-site capture stays blocked, and slopes, gateway grade, furniture clearance and low views need variant-specific evidence. |

Runtime commits for the shared pilot are the current worktree ancestry through
`c912761fc`; the exact original model-byte hash is **not independently
recorded in this rehearsal**. New candidate evidence must supply a manifest
hash and source-locked asset review. The park's procedure revision above is
from the picker registry, not a visual approval stamp.

## Gate disposition for the rehearsal

| Shared gate | Result for this scene | Why it does or does not transfer |
| --- | --- | --- |
| C1 identity | Runtime ID/readback PASS; asset-byte review NOT TESTED | Saved IDs and recipes match the selected variants, but this record did not independently hash/review the GLB. |
| C2 dimensions | Prepared scene PASS; full supported range NOT TESTED | Native house scale and street width are fixed; only a bounded set of plot sizes and one park resize were exercised. |
| C3 ground | Prepared scene PASS; natural full-site FAIL | Explicit 1102.62 m level and measured concept edges support the classroom export. Eight natural cells remain uncertain; hillside capture must reject them. |
| C4 freshness | PASS for observed edits | Ground/recipe invalidation, UI move, park remeasurement, reload and strict capture rejection were observed. New geometry still requires a fresh run. |
| C5 circulation | Prepared house approach PASS; natural route FAIL | Front step reaches the street on prepared ground. The natural-house approach intersects measured terrain. Park west entrance solved after relocation, but its low-view grade was not approved. |
| C6 editing | PASS for observed controls | Place, Move, park resize, Undo/Redo and reload were tested. This does not establish every transform on every candidate. |
| C7 recovery | PASS for bounded errors | Boundary rejection, rear-step facing, locked-recipe conflict and partial-ground capture all kept saved design recoverable. |
| C8 visibility/capture | Prepared exact preview PASS; paid AI fidelity NOT TESTED | Browser preview shows the mixed scene and route. A real generated image was not run in the isolated harness. |
| C9 student use | PASS for this prepared desktop path | Boundary, street, building, park, front entrance and free preview were authored through ordinary controls. Touch/mobile was not tested. |

The next catalogue candidate should copy
[the per-variant review template](ARCHETYPE_RUNTIME_REVIEW_TEMPLATE.md),
attach its exact asset/procedure identity and a finite evidence manifest,
then run level, slope, edge/recovery, edit, route and capture checks on a
disposable site. Reuse shared test results only when the shared implementation
is unchanged; never transfer this scene's visual approval to a new variant.
Keep the existing separate RLASM 6.1 independent review and human catalogue
activation process.

The remaining pre-scale classroom checks are a bounded real image-fidelity
comparison and an actual **new** building/street/park entry pilot. The prepared
Currie student journey already supplies a working classroom fallback while
natural partial-site capture and the park's minimum-size programme remain
recorded limits.
