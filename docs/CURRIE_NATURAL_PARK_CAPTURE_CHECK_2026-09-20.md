# Currie natural park and capture check — 20 September 2026

Scope: disposable UI-authored Currie project
`0320bb4f-395c-41c6-a6c0-ad28bb1572ef`. The protected vacant reference
was not edited. A complete prepared-mode zone backup was saved outside Git
before this bounded trial and restored afterward.

On Follow existing terrain, eight shared-site cells again remained uncertain
and the original 40 × 35 m neighbourhood park disappeared. Using ordinary
Reshape and Move controls, the park was narrowed to its supported 30 m width
and moved west onto repeatably measured ground. It became visible in the
overhead scene. Review ground reported **1 of 1 parks** with repeatable
measurements and enabled Use measured park terrain. The student selected the
west entrance edge and shared-street target; the shared park access solver
reported one connection, and the entrance was saved. The narrower park has a
reduced programme: zero play pockets, pavilion or swing. It is a recovery
option, not evidence that the original full programme fits this location.

Use measured park terrain saved a park-specific profile and switched the site
to the hillside (`landscape`) mode. Before this source change, that mode made
the shared-site verifier inactive even though the surrounding natural tiles
still contained eight uncertain cells. Review entrances had no current ground
measurements, yet the free exact 3D export succeeded. This could present a
scene with an unchecked house approach as complete.

The shared provider now samples and verifies a hillside site whenever its
original tiles remain visible. A park-specific measured profile proves the
park, not the entire site. After the fix and reload, the park remained visible,
the eight-cell warning returned, Review entrances correctly reported that the
saved house approach intersects measured terrain, and the exact export had no
Download render link. The error directs the student to Review ground. No
measurement thresholds, unknown heights, park geometry rules, or capture
freshness checks were relaxed.

The prepared 1102.62 m classroom fixture, its original 40 × 35 m park,
measured concept retaining edges and saved front house approach were restored
from the backup. Readback confirmed geometry and key properties; the park's
automatic 3D representation recompiled after restoration. Reload showed the
complete prepared scene and the free exact preview passed. The natural
hillside/partial-site combination remains a **design and capture follow-up**;
use the explicit prepared level for this parcel's complete student presentation.

Verification: 28 focused tests in three frontend suites, TypeScript checking,
and browser reload/export checks passed. Evidence is outside Git under
`C:/dev-artifacts/CityPrompt/grounding-batch-a/park-natural-*` and
`landscape-guard-*`.

For each future park variant, separately verify its entire measured footprint,
its actual programme at the supported minimum size, its gateway and street
route, and the whole proposal's capture readiness. Do not infer whole-site or
building-entrance support from a repeatable park profile. Where measured
terrain is incomplete, keep exact export blocked or choose an explicit prepared
site and review its edges.
