# Currie shared-street access check — 20 September 2026

The disposable student-authored project `0320bb4f-395c-41c6-a6c0-ad28bb1572ef`
was reopened in the browser without changing its saved zones. The six-metre
`yield_street_v0` was inspected from a closer oblique camera and an overhead
context camera. The red-brown brick surface, alternating edge planters, flush
edge treatment and yield signs match its Dutch woonerf runtime contract. The
straight shared surface remains visible between its chicanes. A vehicle-turning
or emergency-clearance measurement was not made, so this is a visual concept
check rather than a clearance approval.

The saved street polygon ends inside the site at both ends. It has no
`connect_to_public_road` marker, and the overhead view shows no junction to the
surrounding public streets. The previous AI image therefore did not invent a
missing off-site road connection; the **authored design itself is incomplete**
for a vehicle-access demonstration. Do not change the woonerf renderer or relax
image-fidelity thresholds to hide this. A student can use the existing street
settings to mark a proposed public-road connection and drag one end toward a
road, within the bounded 30 m off-site allowance. The marker itself cannot
prove that the endpoint meets a real mapped road.

The shared Present step now gives a nonblocking access notice for any design
with authored streets. It distinguishes no marked connection, a marker without
a valid short off-site route, and a geometrically proposed off-site
connection that still needs visual confirmation against the map. A live browser
check on this Currie project showed the first message while the Image and Video
buttons remained available. This check transfers to future street variants;
new variants still need their own section, clearance, junction, grade and image
review.

Evidence outside Git: `C:/dev-artifacts/CityPrompt/grounding-batch-a/currie-clear-route/`
contains closer oblique, overhead context, and Present-step screenshots plus a
read-only zone snapshot. The protected original Currie project was untouched.
