# Currie student trial — 2026-09-23

Disposable project: **Currie — Curved Streets Student Trial 2026-09-23** (`81da9efb-43bc-4a3f-8bf2-98d86126c50b`). Local URL while the trial server is running: `http://127.0.0.1:5178/projects/81da9efb-43bc-4a3f-8bf2-98d86126c50b`.

## Exercise result

- Started at the vacant CFB Currie field and drew a nine-corner, 4.7 ha boundary between the curving west road and straight east road. Chose **Clear site for redevelopment**.
- Placed five buildings: a Beltline brick mixed-use mid-rise, two modern infill homes and two post-war bungalows. Added a neighbourhood park with a pavilion and play area.
- Drew two 16 m Calgary local streets. Bent the main route by dragging its control point, made an internal T-junction, then extended the east branch to the mapped road edge using **Connect to a public road**. The Present screen recognized the proposed connection.
- Undid and redid a bungalow placement. The first Redo exposed a server failure restoring a deleted compiled-building link; the fix in `backend/app/api/v1/site_zones.py` was verified with focused tests and a fresh browser Undo/Redo sequence.
- Generated and applied **Neighbourhood gardens** (48 trees; about 37,310 m² of residual landscape after the road edit). The design and compiled landscape survived a page reload. In a close view, the detailed mid-rise, park furniture, sidewalks and planting resolved; at wide scale the five homes look small on a 4.7 ha site.
- **Export current 3D view · free** generated an exact, non-AI preview in roughly four seconds in the 1280×720 in-app browser. In ultrawide Edge, an offsite view timed out at about 53 seconds with a ground-alignment message; after **Focus Plan**, the preview completed in about 18 seconds.

## Image trial and export follow-up

- Generated two GPT Image 2.5 Sunburst images in the same disposable project: a close photorealistic park/mixed-use view and a wider documentary neighbourhood view. Both AI originals are visually appealing and were saved alongside their exact 3D source images in **Project Renders** (four saved files). The site, main building, bent street, T-junction and park remain recognizable in the documentary original, but the automatic fidelity checks flagged changed source structure and correctly returned the exact 3D capture as the safe result. Treat the AI originals as **illustrative, unverified** studies until visually reviewed; do not present them as exact design evidence.
- Follow-up side-by-side gallery review found no obvious wholesale relocation in either composition. The wide finish preserves the road network, central building and park location while substantially changing the site lawn and tile transition. The close finish preserves the near road, sidewalk, main building and oval park in broad form. Smaller facade, planting, path and furniture details still need a more exact comparison against the source; this visual pass does not override either failed gate.
- Offline review of the four saved PNGs confirms that the wide AI image keeps the authored street, building and park layout while restyling nearly every vegetated and background pixel. The close image retains the same broad composition, but its tree crowns, facade details and paths move enough that a full-frame edge comparison fails. That comparison lacks the original proposal and instance masks, so it cannot diagnose the actual gate or justify changing its limits. Future paid Direct 3D gallery saves now attach the gate diagnostics and warnings to both provenance sidecars. Keep these two originals marked illustrative/review-required; a subsequent masked review can separate planting texture from moved authored geometry.
- The first call used 212 tokens from a 328-token local test balance. A standard 1280×720 viewport brought the second call to 102 tokens. With 14 tokens left, a third paid 2.5 call was unavailable. A third, free exact 3D capture from overhead showed the irregular parcel, all five buildings, two streets and park, including the east public-road tie-in.
- The saved-render **Download render** link initially failed because `C:\Users\andre\Downloads` was a junction to unmounted `D:\C-drive-archive\Downloads`. The junction was renamed to `Downloads.broken-d-junction-2026-09-23` and replaced with a real folder on C:. A saved source render then downloaded as a valid 1280×656 PNG (1,181,917 bytes).
- The free exact-view preview's large data-URL link did not download in the in-app browser, even with the repaired folder. Its image download now uses a temporary Blob URL. The same control in Edge wrote a valid 2048×742 PNG (3,534,944 bytes) into Downloads. A second in-app trial showed the exact PNG preview and completed its download action, but wrote no new file to Downloads. Its host-specific handling remains unconfirmed for students using a regular browser.
- The typed-location error from project creation is fixed in `ProjectListPage.tsx`: creation now resolves a typed address before saving, and blocks with an actionable message if no location can be found. Two focused tests cover successful resolution and failure; frontend type-check passes.
- Boundary context enrichment is optional after the boundary itself saves. An Overpass connection/HTTP failure now returns a specific 503 without altering the saved boundary, and the editor shows a neutral "you can keep designing" notice instead of a raw 500 error toast. A controlled failure test confirms the saved zone remains in the editor; the original transient failure has not been reproduced against live Overpass.
- Recovery storage audit found that 20 of 28 Model Library GLBs were absent from the backend's `cityprompt-studio` bucket but present under identical keys in `student-studio-2027`. Those 20 missing keys were copied without overwriting the eight existing objects; a read-only check then reported 28/28 present. `scripts/check_model_library_storage.py` now makes this configured-bucket check repeatable for future building variants. This repairs only the local recovery storage, not remote deployment storage.

## Release checks and verifications

1. **Image fidelity:** Both 2.5 finishes failed the automated source checks despite plausible visual results. The saved originals were reviewed, but their original gate metrics and semantic masks were not retained. New saves retain gate diagnostics in provenance; use a masked source/original comparison on the next bounded image pilot to determine whether vegetation, lawn and tile blending are causing false positives, while retaining strict checks for building footprints, street topology and new structures. Do not loosen thresholds solely to pass these examples.
2. **PNG download:** Saved-render download is verified in the in-app browser; free exact-view download is verified in Edge. The embedded browser still does not write the free exact PNG to Downloads, even though its preview and download action complete. Confirm whether that host needs a separate delivery path before treating its export as supported.
3. **Site save feedback:** Saving the initial boundary showed a context-load 500 toast even though the boundary persisted and design continued. The optional-service failure path now has a controlled regression test and clearer feedback. Confirm it once in a fresh browser boundary exercise; the original live service failure remains unconfirmed.
4. **Local asset setup:** The configured-bucket checker passes for all 28 current Model Library GLBs in this recovery environment after copying 20 missing keys. Run it against the classroom deployment bucket before release; this local repair does not establish that remote storage is complete.
5. **Landscape after edits — verified locally:** The full browser path exposed a backend regression: automatic 3D recompilation removed the stale landscape recipe and switched the boundary to `placed_objects_only`, hiding the Present refresh notice. Object recompilation now preserves a student-generated compiled or stale recipe while respecting an explicit landscape removal. In the Currie browser trial, adding a street bend showed **Refresh site landscape** and disabled Image/Video; the button opened the Site panel, and applying a fresh preview reenabled both outputs. The temporary route edit was undone and Neighbourhood gardens reapplied. The disposable project ends with a compiled landscape and Present enabled. No paid image calls were made in this follow-up.

The focused backend landscape cases pass (6/6). The broader `test_lego_assembly.py` run had 114 passes and 71 failures. The first failures are missing generated family manifests, including `frontend/public/families/deco-theater-mainstreet/deco-theater-mainstreet_manifest.json`; those catalogue-asset cases remain outside this landscape fix.

The street and site geometry are ideation-scale. This trial checked visual continuity and normal student operations, not survey-accurate access, legal parcel lines or construction-standard intersection geometry.
