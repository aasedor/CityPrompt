# Image fidelity checkpoint — 16 September 2026

The image panel now respects the existing server fidelity decision. Previously,
an `authoritative_source` fallback could be replaced in the client by the
rejected provider original and described as ready to use. The primary result
now keeps the returned frame, its matching saved record and output fingerprint.
The AI original remains a separately labelled, explicit inspection choice.

Failure, incomplete evidence and passed image checks have different messages.
Passing checks never claims exact 3D identity. Failed finishes cannot be accepted
as a new successful image. Already-persisted results cannot be saved twice.
The result and source comparison appear before controls; changing image options
is required before another paid attempt. Diagnostic detail stays secondary.
Saved thumbnails and enlarged images retain source/unverified distinctions.

## Verification

- 39 narrow frontend cases across fidelity status, response selection, panel
  contracts and gallery notices; TypeScript and changed-file ESLint pass.
- One local synthetic provider returned a solid red frame through the actual
  authenticated API, validation and persistence path. The server rejected it;
  the UI showed the original 3D frame. No external provider was called.
- Reloaded gallery evidence preserves both records and their distinct notices.
  Desktop, laptop and narrower result views were inspected. Returning to options
  restores the preset/toggles without a retry. No automatic paid retry occurred.
- One real GPT Image 2 realistic request used the frozen Gold Standard aerial
  camera, with people and vehicles off. Source, raw provider output, returned
  composite and UI result were visually inspected. Camera position/quaternion
  remained exactly equal. Server inventory: 16 buildings, five street/junction
  instances, two parks and one ground instance. Available checks passed on 13
  macro components, with 96.2% silhouette/topology agreement; these are image
  proxies, not an architectural certification.
- Saved provenance retrieved through the authenticated API retains matching
  plan/camera hashes, capture/output fingerprints, timestamp and explicit
  presentation choices. No schema migration or duplicate scene model was added.

## Visual judgement and limits

The raw candidate has improved sky, lighting and context appearance. The returned
image preserves source context and retains the broad authored building, park and
street arrangement. The current composite has only a modest finish improvement;
it does not pass the mission's professional-quality requirement. Aerial scale
also does not prove detailed RLASM identity, entrance contact or pedestrian quality.
The existing prepared-site edge defect remains visible. Do not weaken checks or
replace the benchmark to claim these gates passed. Additional paid views should
follow an evidence-backed improvement, not a blind retry.

Local evidence is under `C:/dev-artifacts/CityPrompt/student-design-transformation/`:
`PHASE11-PAID-SOURCE.png`, `PHASE11-PAID-PROVIDER.png`, `PHASE11-RETURNED.png`,
`PHASE11-PAID-RESULT.png`, `PHASE11-FINAL-FAILURE.png`, reload/viewport screenshots,
`phase11-paid-result.json`, gallery metadata and individual provenance records.
The synthetic red image is failure-test data, never a real generation result.

## Spend

The one real image reported 670 text input, 10,150 image input and 4,950 image
output tokens. At the [published standard rates](https://developers.openai.com/api/docs/pricing),
the conservative usage-derived estimate is **US $0.116525** (not an account invoice).
The local ledger also reserves US $2 for cumulative Maps validation. Total spent
estimate plus reserve is **US $2.116525**, below the US $10 hard ceiling. Paid
provider access was disabled after this bounded test. No paid video was called.
